{% extends 'base.html.twig' %}

{% block title 'New event' %}

{% block body %}
    <section class="container row row-cols-1 enter justify-content-center">
        <div class="col-md-12 mx-auto text-center">
            <h1>New event</h1>
        </div>

        <twig:NewEventForm class="w-50 m-auto" event="{{ event }}" />
    </section>
{% endblock %}
